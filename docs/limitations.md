# Limitations
- Demo mode uses static CSV/GeoJSON; no live inference or DB persistence
- No authentication/authorization; add OAuth/SAML for municipal deployment
- Upload pipeline stubbed; integrate object storage and background workers
- Recommendation rules are deterministic; calibrate with real field data and cost models
- No unit/integration tests included
